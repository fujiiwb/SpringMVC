<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper namespace='br.com.spring.mapper.object.SQLAdapterMapper'>
	<select id="executeSQL" parameterType="SQLAdapter">
		${sql}
	</select>

	<select id="executeSQL_Long" parameterType="SQLAdapter" resultType="long">
		${sql}
	</select>

	<select id="executeSQL_Object" parameterType="SQLAdapter" resultType="object">
		${sql}
	</select>
</mapper>